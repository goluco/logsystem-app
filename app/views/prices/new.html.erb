<h2>Adicionar taxa</h2>

<% if @price.errors.any? %>
  <p>Verifique os erros abaixo</p>
  <ul>
    <% @price.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>