<!DOCTYPE html>
<html>
  <head>
    <title>LogsystemApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
    <header>
      <nav>
        <% if user_signed_in? %>
          <p><%= link_to 'Início', root_path %></p> <br/>
          <%= current_user.email %> 
          <p class='logout_button'><%= button_to 'Sair', destroy_user_session_path, method: :delete %><p> 
        <% elsif admin_signed_in? %>
          <%= current_admin.email %> | 
          <%= button_to 'Sair', destroy_admin_session_path, method: :delete %>
        <% else %>
          <p class='login_button'><%= link_to 'Entrar como admin', new_admin_session_path %> |
          <%= link_to 'Entrar como usuário', new_user_session_path %></p>
        <% end %>
      </nav>
    </header>
    <nav>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </nav>
  </body>
</html>
